#include "main.h"

/**
 * _abs - computes the absolut.
 * @n: the integer to compute the absolute value of.
 *
 * Return: the absolute value of n.
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
