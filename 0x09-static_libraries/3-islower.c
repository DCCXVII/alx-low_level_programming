#include "main.h"

/**
 * _islower - checks for a lowercase character.
 *
 * @c: input character
 *
 * Return: 1 if c is lowercase, 0 otherwise.
 */

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
